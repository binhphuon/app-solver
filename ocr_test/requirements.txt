# Primary OCR engine (default). ONNX-based, dùng models của PaddleOCR
# nhưng nhẹ hơn (no PaddlePaddle dependency). Accuracy tốt nhất cho text nhỏ.
rapidocr-onnxruntime>=1.3.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# Optional alternative engine — bỏ comment nếu muốn test với EasyOCR
# easyocr>=1.7.0
